// function to block the stack for x number of millise
setTimeout(() => {
  // remove the class that hides the element
  document.querySelector("form-input").classList.remove("display-none");
}, 8000);
